# Codex Git/GitHub Workflow Pilot

This is a disposable public repository used to verify the Codex Git/GitHub
workflow: SSH Git operations, CI, protected default-branch rules, pull
requests, and SHA-pinned auto-merge.

It contains no production code, credentials, deployment configuration, or
real project data.
